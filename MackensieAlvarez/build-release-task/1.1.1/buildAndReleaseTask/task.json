{
    "$schema": "https://raw.githubusercontent.com/Microsoft/azure-pipelines-task-lib/master/tasks.schema.json",
    "id": "0f1eba27-a7a6-4788-ae53-fdc47541e6cf",
    "name": "HelmSecrets",
    "friendlyName": "Helm Secrets via Variable Groups",
    "description": "Generate values.yaml based on Variable groups in AZDO",
    "helpMarkDown": "",
    "category": "Utility",
    "author": "Mackensie Alvarez",
    "version": {
        "Major": 1,
        "Minor": 2,
        "Patch": 0
    },
    "instanceNameFormat": "Generate Helm Values",
    "inputs": [{
            "name": "file",
            "type": "filePath",
            "label": "Helm Values YAML",
            "defaultValue": "kube/mackensiealvarez.yaml",
            "required": true,
            "helpMarkDown": "This should be the path of your helm values that you want to append secrets too"
        },
        {
            "name": "variables",
            "type": "string",
            "label": "Variables to include, seperated by comma",
            "defaultValue": "APP_KEY",
            "required": true,
            "helpMarkDown": "We will use this to know which variables to include inside of the YAML"
        },
        {
            "name": "mainNode",
            "type": "string",
            "label": "Node to append variables to. Ex: env",
            "defaultValue": "env",
            "required": true,
            "helpMarkDown": "This is the node inside of the YAML that the values will be appended too"
        }
    ],

    "dataSourceBindings": [],


    "execution": {
        "Node10": {
            "target": "index.js"
        }
    }
}